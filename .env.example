# Brand (lernen | unlearn | loschke | prototype | aok)
NEXT_PUBLIC_BRAND=lernen

# Logto Auth (OIDC)
LOGTO_APP_ID=
LOGTO_APP_SECRET=
LOGTO_ENDPOINT=https://auth.lernen.diy
LOGTO_BASE_URL=http://localhost:3000
LOGTO_COOKIE_SECRET=mindestens-32-zeichen-langer-geheimer-schluessel

# Neon Database
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Vercel AI Gateway (Chat Feature)
AI_GATEWAY_API_KEY=

# Feature Flags
# Steuert sowohl Sidebar-Chat als auch Fullpage-Assistent
NEXT_PUBLIC_CHAT_ENABLED=true

# Dark Mode (opt-out: entfernen oder "false" setzen zum Deaktivieren)
# NEXT_PUBLIC_DARK_MODE=false
# Default Theme: "light" | "dark" | "system"
NEXT_PUBLIC_DEFAULT_THEME=light

# Cloudflare R2 Storage (optional)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_DOMAIN=assets.lernen.diy

# Web Search/Fetch Provider (optional, default: firecrawl)
# Search and fetch can use different providers, e.g. Perplexity for search + Jina for fetch
# SEARCH_PROVIDER=firecrawl
# FETCH_PROVIDER=jina
FIRECRAWL_API_KEY=
# JINA_API_KEY=
# PERPLEXITY_API_KEY=
# TAVILY_API_KEY=

# MCP - Model Context Protocol (optional)
# Set to "true" to enable MCP client support. Individual servers are configured
# in src/config/mcp.ts and gated by their own env vars.
# MCP_ENABLED=true

# Memory System (M8) — Mem0 Cloud
# MEM0_API_KEY=                         # Mem0 Cloud API Key (Feature-Gate)
# MEMORY_SEARCH_LIMIT=10               # Max Memories im Prompt (optional, default: 10)
# MEMORY_MIN_MESSAGES=6                # Min Messages vor Auto-Extraktion (optional, default: 6)

# Business Mode (M9) — Datenschutz & PII-Erkennung
# NEXT_PUBLIC_BUSINESS_MODE=true       # Feature-Gate (opt-in)
# BUSINESS_MODE_PII_DETECTION=regex    # PII-Erkennung (default: regex)
# BUSINESS_MODE_PRIVACY_URL=           # Link zur Datenschutzerklaerung
# BUSINESS_MODE_EU_MODEL=mistral-large-latest  # EU-konformes Modell (Mistral)
# MISTRAL_API_KEY=                     # Mistral API Key (fuer EU-Routing)
# BUSINESS_MODE_LOCAL_MODEL=llama3.1   # Lokales Modell (Ollama etc.)
# BUSINESS_MODE_LOCAL_URL=http://localhost:11434/v1  # OpenAI-kompatibler Endpoint
