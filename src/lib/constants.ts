/**
 * Shared constants used across multiple API routes.
 */

export const MAX_MESSAGE_LENGTH = 8000
export const MAX_MESSAGES = 50
export const SLUG_PATTERN = /^[a-z0-9-]+$/
export const MAX_BODY_SIZE = 8 * 1024 * 1024 // ~8MB
